{{page-title 'Home'}}

<div
  class='route-container'
  style='background: url({{this.backgroundImgUrl}});'
>
  <nav class="navbar navbar-light home-navbar">
    <LinkTo @route="settings" class="settings-link">
      <Icons::Gear/>
    </LinkTo>
  </nav>

  <SearchBar />
  {{outlet}}
</div>
